# Database Configuration
POSTGRES_PASSWORD=postgres

# JWT Configuration - IMPORTANT: Change this in production!
# This is a 32-character secret for JWT signing
JWT_SECRET=your-super-secret-jwt-token-with-at-least-32-characters

# Supabase Keys
# These need to be generated using your JWT_SECRET
# You can use https://supabase.com/docs/guides/hosting/overview#api-keys
# or generate them manually with the JWT_SECRET
SUPABASE_ANON_KEY=your_supabase_anon_key_here
SUPABASE_SERVICE_KEY=your_supabase_service_key_here

# Google API Key for agent-planner-agents
# Get from https://ai.google.dev/
GOOGLE_API_KEY=your_google_api_key_here

# API Tokens for inter-service communication
# These need to be generated after the system is running
# Use the create_api_token.sh script in agent-planner/scripts/
PLANNING_API_TOKEN=your_planning_api_token_here
USER_API_TOKEN=your_user_api_token_here

# URLs (usually don't need to change for local development)
SUPABASE_URL=https://supabase.agent-planner.localhost

# Optional: Host IP binding (defaults to 127.0.0.1)
# Uncomment to bind to all interfaces (useful for network access)
# HOST_IP=0.0.0.0